# AI 重建上下文

用户要求彻底排查并升级 AI，明确反对未调研就直接重写、未验证就声称完成。2026-09-22 已授权执行调研报告中的对照与后续实现。

月灵是 Go QQ 机器人，已有群插件开关、权限、确认、PostgreSQL 群历史、个人记忆及业务工具。已运行 AstrBot/LangBot 的固定版本组件与接口对照。二者仍需动作去重、取消/回复归属和正文过滤等接入工作，当前选择保留 Go 运行职责并拆分总结流程、会话、工具执行及模型协议；暂不直接外接候选。结论不扩展为产品整体稳定性排名，详见 docs/ai-evaluation.md。

模型协议包含提供商推理字段；完整 assistant/tool 往返应保留，用户只接收最终正文。群历史查询必须按群和上海时区限定，历史取样不能声称完整覆盖。

使用虚构资料与假模型验证故障，不发送真实群聊到候选产品。当前配置真实模型的 ai-check 也仅使用虚构资料。临时环境位于 /private/tmp/yueling-eval-*。已有 config.toml 含真实凭证，调研与测试不能打印其内容。2026-09-22 用户已授权提交、创建标签并推送，本批次使用 v1.22.0；现有标签工作流会构建并发布 Docker 镜像，没有直接操作生产实例的授权。

本轮工作纳入 v1.22.0；该版本也包含此前 main 上的订阅轮询修复提交 5f06adf。真实 NapCat 联调和原生产故障日志仍未取得；模拟/组件测试不能代替生产验收。

本轮 Docker 管理接口超时，不能重启其他服务；本地专用测试库未取得有效连接。本轮 Go 数据库用例跳过，与更早带数据库通过的测试记录分开。代码测试会临时使用系统 PingFang 字体链接，结束后清理。
